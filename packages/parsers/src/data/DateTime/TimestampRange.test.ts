import { describe, it, test } from "vitest";

describe("TimestampRangeConstructor", () => {
	test.todo("_parse(...)", () => {
		//
	});

	test.todo("isRange(...)", () => {
		//
	});
});

describe("TimestampRange", () => {
	test.todo("_equals(...)", () => {
		//
	});

	test.todo("_isWithinRange(...)", () => {
		//
	});

	test.todo("toString()", () => {
		//
	});

	test.todo("toJSON()", () => {
		//
	});

	test.todo("get lower()", () => {
		//
	});

	test.todo("set lower(...)", () => {
		//
	});

	test.todo("get upper()", () => {
		//
	});

	test.todo("set upper(...)", () => {
		//
	});

	test.todo("get values()", () => {
		//
	});

	test.todo("set values(...)", () => {
		//
	});

	test.todo("get empty()", () => {
		//
	});

	test.todo("get value()", () => {
		//
	});

	test.todo("set value(...)", () => {
		//
	});

	test.todo("get postgres()", () => {
		//
	});

	test.todo("set postgres(...)", () => {
		//
	});
});

describe("PostgreSQL", () => {
	it.todo("should work with PostgreSQL's own tests", () => {
		//* https://github.com/postgres/postgres/blob/master/src/test/regress/sql/rangetypes.sql
	});

	it.todo("should be returned from PostgreSQL", async () => {
		//
	});
});
