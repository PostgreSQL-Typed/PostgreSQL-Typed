{
	"name": "postgresql-caching",
	"version": "1.0.0",
	"description": "PostgreSQL with automatic Caching functionality using Keyv",
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"repository": "https://github.com/PostgreSQL-Typed/PostgreSQL-Caching",
	"files": [
		"lib"
	],
	"scripts": {
		"dev": "tsc -w",
		"build": "tsc",
		"test": "vitest --run",
		"test:watch": "vitest --ui --coverage",
		"test:ci": "vitest --run --coverage",
		"lint": "eslint --fix --ext .ts ./ && prettier -w .",
		"lint:ci": "eslint --ext .ts ./ && prettier --check ."
	},
	"keywords": [
		"postgresql",
		"caching",
		"automatic",
		"keyv"
	],
	"author": "Bas950",
	"license": "MPL-2.0",
	"devDependencies": {
		"@types/jest": "^28.1.6",
		"@types/pg": "^8.6.5",
		"jest": "^28.1.3",
		"source-map-support": "^0.5.21",
		"ts-jest": "^28.0.7",
		"typescript": "^4.7.4",
		"postgresql-typed": "workspace:*"
	},
	"dependencies": {
		"keyv": "^4.3.3",
		"pg": "^8.7.3"
	},
	"peerDependencies": {
		"postgresql-typed": "workspace:*"
	}
}
