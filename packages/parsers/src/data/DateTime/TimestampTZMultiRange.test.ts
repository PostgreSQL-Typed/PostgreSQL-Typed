/* eslint-disable unicorn/filename-case */
import { describe, it, test } from "vitest";

describe("TimestampTZMultiRangeConstructor", () => {
	test.todo("_parse(...)", () => {
		//
	});

	test.todo("isMultiRange(...)", () => {
		//
	});
});

describe("TimestampTZMultiRange", () => {
	test.todo("_equals(...)", () => {
		//
	});

	test.todo("toString()", () => {
		//
	});

	test.todo("toJSON()", () => {
		//
	});

	test.todo("get ranges()", () => {
		//
	});

	test.todo("set ranges(...)", () => {
		//
	});

	test.todo("get value()", () => {
		//
	});

	test.todo("set value(...)", () => {
		//
	});

	test.todo("get postgres()", () => {
		//
	});

	test.todo("set postgres(...)", () => {
		//
	});
});

describe("PostgreSQL", () => {
	it.todo("should work with PostgreSQL's own tests", () => {
		//* https://github.com/postgres/postgres/blob/master/src/test/regress/sql/multirangetypes.sql
	});

	it.todo("should be returned from PostgreSQL", async () => {
		//
	});
});
